import Nav from "./Nav";
import ColorModeButton from "./ColorModeButton";

export default function Header() {
    return (
        <header>
            <h2>HEADER</h2>
            <ColorModeButton />
            <Nav />
        </header>
    );
}
